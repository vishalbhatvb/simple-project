apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Data-Entry-Form.fullname" . }}-test-connection"
  labels:
    {{- include "Data-Entry-Form.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Data-Entry-Form.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
